# Runtime deps for v1. Nothing here is needed for the domain layer (step 1) —
# it imports only the stdlib. Web/persistence/checker steps will start using these.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.20.0
apscheduler>=3.10
httpx>=0.27  # also the runtime HTTP client the checker uses to ping monitored URLs
passlib[bcrypt]>=1.7
bcrypt<4.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1
python-jose[cryptography]>=3.3
pydantic-settings>=2.0
email-validator>=2.0
